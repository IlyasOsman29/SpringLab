# SpringLab
Demonstrates Spring constructor injection for collections of `IGamePluginService` and `IEntityProcessingService`. The `Game` class never constructs Player or Asteroid components itself. Run `mvn exec:java`.
